export default function AddFlatPage() {
  return (
    <div>
      read all things carefully build for same fields for flat like as room but only change api end points but not change fields and
       also change validation controllers and take same api 
    </div>
  );
}